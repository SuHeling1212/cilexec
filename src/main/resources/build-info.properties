application.name=CilExec
application.version=${project.version}
build.revision=${build.revision}
fcl.runtime.format=${fcl.runtime.format.version}
database.schema.minimum=${database.schema.minimum}
database.schema.maximum=${database.schema.maximum}
