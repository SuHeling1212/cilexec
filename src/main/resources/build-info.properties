application.name=CilExec
application.version=${project.version}
build.revision=${build.revision}
fcl.runtime.format=${project.version}
database.schema.minimum=${project.version}
database.schema.maximum=${project.version}
