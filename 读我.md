# CilExec

> **声明：本项目由 AI（TREA,DeepSeek）开发完成，甚至连文档也是 AI 生成的。我不对代码质量和问题负任何责任**

一个单一二进制可执行文件纯磁盘教学操作系统，使用 Java 实现。

## CilExec 是什么？

**核心理念：万物皆文件，状态持久化**

CilExec 将 Unix 的"万物皆文件"理念贯彻到**极致**：

- 💾 **纯磁盘 I/O** - 磁盘是主要的读写设备，所有状态持久化存储
- 📁 **所有状态文件化** - 系统状态和进程都作为文件存储在磁盘中
- 🔄 **内存操作透明化** - 运行时数据在内存中处理，但自动同步到磁盘
- 🚫 **不可引导** - 无法作为真实操作系统被 BIOS 引导（但这无所谓，只是教学用）awa

## 架构设计

### 单一可执行文件

CilExec **只有一个可执行文件**，包含：

- ✅ 安装程序
- ✅ 启动程序
- ✅ 硬件交互 API
- ✅ 基本系统内核

### 为什么用 Java？

因为 **JVM 太好用了！** 用户可以通过调用 CilExec 的 API 来操纵硬件。虽然效率会下降，加上磁盘 IO 的超慢速度……但这只是一个教学系统 awa

## 状态持久化的"好处"

### 🛡️ "更好的空安全！"

> *doge*

没有直接内存操作 = 没有指针 = 没有空指针异常！

事实上……变量、数组、方法虽然定义在进程文件内部，但你还是可能会访问到不存在的变量。

### 🔒 "更好的数据安全！"

> *doge*

状态持久化 = 不用担心断电数据丢失！

**但说真的：** 得益于状态持久化设计，你可以直接修改进程数据：

1. 保存状态（文件）
2. 关闭系统
3. 在真实系统上打开文件
4. 进行修改
5. 重启 CilExec
6. 自动加载"内存"继续工作

## 它实际上是什么

CilExec 是一个概念验证项目，用 Java 实现操作系统核心机制：

- 进程管理
- 文件系统
- 脚本引擎
- 权限框架

## 技术栈

- Java 25
- Maven
- Gson (JSON 处理)

## 脚本格式

**`.fcl`** (Follarce CilExec Language) 是标准的系统脚本格式。

示例：

```fcl
# 这是注释
while true {
    # INIT 进程主循环
}
```

## 构建和运行

```bash
# 使用 Maven 打包
mvn clean package

# 或使用提供的脚本
./package.sh

# 运行
java -jar target/cilexec-1.0.2-SNAPSHOT.jar
```

## 项目性质

**这是一个操作系统内核的概念验证项目。**

它实现了操作系统的核心机制，但本身不是一个完整的可用系统：

- ✅ 内核功能完整（进程、文件系统、脚本引擎）
- ❌ 没有 Shell/命令行界面
- ❌ 没有用户登录系统
- ❌ 没有系统工具（ls, cat, echo 等）

## 文档

- [项目结构](doc/zh-CN/项目结构.md) - 项目结构和核心功能
- [架构设计](doc/zh-CN/架构设计.md) - 架构设计原则
- [API参考](doc/zh-CN/API参考.md) - Java API 和脚本语言 API
- [使用示例](doc/zh-CN/使用示例.md) - 使用示例
- [插件开发指南](doc/zh-CN/插件开发指南.md) - 如何扩展功能
- [错误码](doc/zh-CN/错误码.md) - 错误码参考
- [日志系统](doc/zh-CN/日志系统.md) - 日志系统文档

## 用途

- 操作系统教学演示
- 虚拟化技术研究
- 脚本引擎开发参考
- 嵌入式系统原型

## 许可证

本项目采用 [MIT License](LICENSE) 开源许可证。
