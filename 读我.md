# CilExec

> **声明：本项目由 AI（TREA,DeepSeek）开发完成，甚至连文档也是 AI 生成的。我不对代码质量和问题负任何责任**

一个单一二进制可执行文件纯磁盘教学模拟操作系统，使用 Java 实现。

## CilExec 是什么？

**核心理念：万物皆文件，状态持久化**

CilExec 将 Unix 的"万物皆文件"理念贯彻到**极致**：

- 💾 **纯磁盘 I/O** - 磁盘是主要的读写设备，所有状态持久化存储
- 📁 **所有状态文件化** - 系统状态和进程都作为文件存储在磁盘中
- 🔄 **内存操作透明化** - 运行时数据在内存中处理，但自动同步到磁盘
- 🚫 **不可引导** - 无法作为真实模拟操作系统被 BIOS 引导（但这无所谓，只是教学用）awa

## 架构设计

### 单一可执行文件

CilExec **只有一个可执行文件**，包含：

- ✅ 安装程序
- ✅ 启动程序
- ✅ 硬件交互 API
- ✅ 基本系统内核

### 为什么用 Java？

因为 **JVM 太好用了！** 用户可以通过调用 CilExec 的 API 来操纵硬件。虽然效率会下降，加上磁盘 IO 的超慢速度……但这只是一个教学系统 awa

### 进程恢复与副作用

每个进程实例都有不可复用的 `ProcessGeneration`。PID 不能单独作为生命周期操作的授权条件，因此 PID 被回收后，旧进程的消息、锁和父子关系不会作用于新实例。

每条会修改状态的指令都会持久化执行 attempt 和 effect receipt。文件、swap pool、用户和进程等内部副作用会按 receipt 重放，而不会重复执行。HTTP POST 等无法判断外部结果的副作用会进入 `EFFECT_RECOVERY` 阻塞状态，必须由管理员明确确认或拒绝。

进程控制消息保存在按 generation 隔离的磁盘 inbox 中。delivery ledger 将消息 ID 永久绑定到原始目标实例；启动恢复会补发在 ledger 提交后、inbox 消息落盘前中断的消息。fork 预留、生命周期清理、锁租约和 fencing token 也会在调度前恢复和校验。

## 状态持久化的"好处"
> *doge*
### 🛡️ "更好的空安全！"



没有直接内存操作 = 没有指针 = 没有空指针异常！

事实上……变量、数组、方法虽然定义在进程文件内部，但你还是可能会访问到不存在的变量。

### 🔒 "更好的数据安全！"



状态持久化 = 不用担心断电数据丢失！

**但说真的：** 得益于状态持久化设计，你可以直接修改进程数据：

1. 保存状态（文件）
2. 关闭系统
3. 在真实系统上打开文件
4. 进行修改
5. 重启 CilExec
6. 自动加载"内存"继续工作

## 它实际上是什么

CilExec 是一个概念验证项目，用 Java 实现模拟操作系统核心机制：

- 进程管理
- 文件系统
- 脚本引擎
- 权限框架

## 版本

**当前版本：1.0-SNAPSHOT**

这是首个稳定版本，在代码质量、安全性和测试方面进行了全面改进。

### 1.0.0 版本亮点

- ✅ **增强的安全性**：路径验证、权限检查、资源管理
- ✅ **更好的异常处理**：区分可恢复和不可恢复异常
- ✅ **进程集成测试**：覆盖状态机、进程控制、并发 fork 和崩溃恢复
- ✅ **代码质量提升**：空指针检查、常量提取、减少代码重复
- ✅ **改进的文档**：更新错误码和示例

## 技术栈

- Java 26
- Maven
- Gson (JSON 处理)
- JUnit 5 (测试)

## 脚本格式

**`.fcl`** (Follarce CilExec Language) 是标准的系统脚本格式。

示例：

```fcl
# 这是注释
while true {
    # INIT 进程主循环
}
```

## 构建和运行

```bash
# 编译
mvn clean compile

# 运行测试
mvn test

# 打包
mvn clean package

# 运行应用
java -jar target/cilexec-1.0-SNAPSHOT.jar

# 或直接运行
mvn dependency:copy-dependencies -q
java -cp "target/classes:target/dependency/*" com.follarce.Main
```

## 测试

项目包含 JUnit 5 进程集成测试：

```bash
# 运行所有测试
mvn test
```

测试覆盖包括：
- `fork`、`exec`、`kill`、`wait`、`waitPID`、`pause`、`continue`
- 持久化进程状态机与调度器恢复
- 父进程退出后的孤儿收养、多层 fork 与并发子进程退出
- 256 路并发 PID 分配与 PID 回收
- 随机强制终止、独立 JVM 多轮重启与原子进程快照恢复

## 项目性质

**这是一个模拟操作系统内核的概念验证项目。**

它实现了模拟操作系统的核心机制：

- ✅ 内核功能完整（进程、文件系统、脚本引擎）
- ✅ 用户登录系统

## 文档

- [项目结构](doc/zh-CN/项目结构.md) - 项目结构和核心功能
- [架构设计](doc/zh-CN/架构设计.md) - 架构设计原则
- [API参考](doc/zh-CN/API参考.md) - Java API 和脚本语言 API
- [使用示例](doc/zh-CN/使用示例.md) - 使用示例
- [插件开发指南](doc/zh-CN/插件开发指南.md) - 如何扩展功能
- [错误码](doc/zh-CN/错误码.md) - 错误码参考
- [日志系统](doc/zh-CN/日志系统.md) - 日志系统文档

## 项目结构

```
src/
├── main/
│   ├── java/com/follarce/
│   │   ├── Main.java                 # 程序入口
│   │   ├── Constants.java            # 常量定义
│   │   ├── init/                     # 系统初始化
│   │   ├── process/                  # 调度、Runner、恢复、inbox 与 effect journal
│   │   ├── function/                 # 内置函数 Provider 与 effect 策略
│   │   ├── script/                   # FCL 词法、语法和求值
│   │   ├── util/                     # VFS、JSON、用户和路径工具
│   │   ├── exception/                # 可恢复和不可恢复异常
│   │   └── log/                      # 日志
│   └── resources/
│       └── INIT.fcl                  # 初始脚本
└── test/
    └── java/com/follarce/            # 单元测试（新增）
```

## 用途

- 模拟操作系统教学演示
- 虚拟化技术研究
- 脚本引擎开发参考
- 嵌入式系统原型

## 许可证

本项目采用 [MIT License](LICENSE) 开源许可证。
